{% render 'partials/head.liquid', title: 'Squad page' %}

{% comment %} <h2>Students</h2>
<ul>
  {% for person in persons %}
    <li><a href="/student/{{ person.id }}">{{ person.name }}</a></li>
  {% endfor %}
</ul> {% endcomment %}


<header>
  <img src="/logo.svg" alt="logo FDND">
</header>

<main>
  <section>
    <h1>SQUAD PAGE</h1>
    <label for="site search"></label>
    <input type="search">
    <button><img src="/filter_icon.svg" alt="filter"></button>
  </section>

  <div class="cards">
    {% for person in persons %}
      <article class="front-card" style="background-image: url({{ person.avatar }});">
        <h2>{{ person.name }}</h2>

        <p>{{ person.koffie_filter }}</p>
        <button>MEER</button>
      </article>
    {% endfor %}

    {% for person in persons %}
      <article class="back-card">
        <dl>
          <dt>{{ person.name }}</dt>
          <dd>{{ person.koffie_filter }}</dd>

          <dt>BIO</dt>
          <dd>{{ person.bio }}</dd>

          <dt>LINKS</dt>
          <dd><a href="#"></a><a href="#"></a></dd>
        </dl>
        <button>BACK</button>
      </article>
    {% endfor %}
  </div>

  <section class="menu-open">
    <h3>Koffie filter</h3>
    <a href="/cappuccino">Cappuccino</a>
    <a href="/esma_Caramel_Frapu">Esma Caramel Frapu</a>
    <a href="/espresso">Espresso</a>
    <a href="/geen">Geen</a>
    <a href="/irish_coffee">Irish coffee</a>
    <a href="/koffie_verkeerd">Koffie verkeerd</a>
    <a href="/latte">Latte</a>
    <a href="/long">Long black</a>
    <a href="/zwart">Met een gebakje</a>
  </section>

</main>

{% render 'partials/foot.liquid', squads: squads %}